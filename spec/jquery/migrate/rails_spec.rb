RSpec.describe Jquery::Migrate::Rails do
  it "has a version number" do
    expect(Jquery::Migrate::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
